# CLAUDE.md

Every rule for this repository lives in @AGENTS.md.

Nothing else belongs in this file. B6 UI keeps a single, agent-agnostic ruleset so any
assistant (Claude, Cursor, Codex, Copilot, …) reads the same instructions.
